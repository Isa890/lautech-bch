const currentLevel = document.querySelector('.current-level');

function currentLevel(){
  if (currentLevel < 100 && currentLevel >=500){
    console.log('valid');
    else{
      console.log('invalid');
    }
  } if (currentLevel = 100){
    var currentLevel = link.href = '100.html';
  }
}
